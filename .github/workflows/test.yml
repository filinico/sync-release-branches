name: "units-test"
on:
  pull_request:
  push:
    branches:
      - main
      - 'v**'

jobs:
  # unit tests
  units:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: npm test
